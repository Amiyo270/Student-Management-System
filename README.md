# 🎓 Student Management System

## Features
- **Database Management:** Private `__student_list` stores records using class methods  
- **Encapsulation:** Student attributes remain private (e.g., `__student_id`)  
- **Auto Registration:** Constructor automatically saves objects to the database  
- **Validation & Error Handling:** Boolean state checks prevent duplicate operations and loops verify IDs before updates
